import { AponMartValidators } from './apon-mart-validators';

describe('AponMartValidators', () => {
  it('should create an instance', () => {
    expect(new AponMartValidators()).toBeTruthy();
  });
});
